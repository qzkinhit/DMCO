# Data

This directory is ignored by Git except for this note. Keep large CSV files here locally, for example:

```text
data/raw/cancer/cancer_random_missing_30.csv
data/raw/cancer/cancer_data_vectorized.csv
```

Use `scripts/prepare_local_data.py` to extract the original zip into `data/raw`.
